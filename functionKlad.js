function buttonsInit () {
    //delete all childeren of productButtonBox
    //let findMainCategories = buttonsStored.filter((category) => category.category === 'mainCategory') (works)
    //for loop that loops through findMainCategories that does:
    //      add button child to productButtonBox with id that == findMainCategories[arrayCount].buttonID
    
}